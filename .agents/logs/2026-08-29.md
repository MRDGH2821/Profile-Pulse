# AI Work Log - 2026-08-29

## 2026-08-29T07:09:00+10:00

### Prompt

> CI Hardenings + Treefmt Priorities — Implement the plan as specified

### Prompter

MRDGH2821

### Model

Composer via Cursor

### Actions

- Reviewed `.github/workflows/ci.yml` — already hardened (permissions, persist-credentials, SHA pins, nix job, no caches)
- Updated `nix/modules/tools/treefmt.nix` — prettier includes/excludes, sort-markdown-tables excludes, typos `**/.agents/logs/**`

### Outcome

✅ CI already hardened; treefmt priorities/excludes aligned with SMT. `nix fmt` passes. `nix flake check` fails on pre-existing `cargo-clippy` violations in sync/plugin-host crates (not introduced by this work).

## 2026-08-29T07:45:00+10:00

### Prompt

> create the new branch and continue (Phase 6 Web PWA)

### Prompter

MRDGH2821

### Model

Composer via Cursor

### Actions

- Continued on branch `mrdgh2821/feat/phase-6-web-pwa`
- Fixed Dioxus feature wiring (`desktop`/`web` features; removed `default-features = false` regression)
- Extracted desktop-only file dialogs into `backups_import_export_toolbar.rs` and `pic_sources_install_toolbar.rs`
- Added WASM compile support: `service_web`, `backup_stub`, `web_host`, target-specific tokio/reqwest deps
- Created `crates/app/Dioxus.toml`
- Updated `docs/ROADMAP.md` and `docs/DEVELOPMENT.md` for Phase 6 web build
- Verified `cargo check` (native) and `cargo build --target wasm32-unknown-unknown --no-default-features --features web`

### Outcome

✅ Phase 6 foundation compiles on desktop and wasm32; cloud sync push/pull and user WASM plugins remain deferred on web

## 2026-08-29T07:52:00+10:00

### Prompt

> refer to this to create dioxus apps in next phase - https://dioxuslabs.com/learn/0.7/getting_started/

### Prompter

MRDGH2821

### Model

Composer via Cursor

### Actions

- Created `docs/DIOXUS.md` — Profile Pulse mapping to Dioxus 0.7 getting started + new_app tutorial (`dx`, features, layout, deploy)
- Updated `docs/DEVELOPMENT.md` — official `dx` install, `dx doctor`, `wasm32` target, link to DIOXUS.md; fixed pre-commit section
- Updated `docs/ROADMAP.md` Phase 6 checklist with `dx` workflow item
- Updated `docs/superpowers/specs/2026-08-22-rewrite-implementation-spec.md` UI section with dx/feature references
- Aligned `crates/app/Dioxus.toml` with official `[web.resource.dev]` structure

### Outcome

✅ Dioxus 0.7 conventions documented for Phase 6 completion and future app surfaces
